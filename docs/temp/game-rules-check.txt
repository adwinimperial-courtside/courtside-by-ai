GAME_RULES COLUMN CHECK
=======================
Date: 2026-04-15
Search: "game_rules" across all files in supabase/migrations/

RESULT: NOT FOUND
-----------------
The string "game_rules" does not appear in any migration file.

No migration adds a game_rules column to the games table.


CURRENT games TABLE DEFINITION (from 20260415000000_initial_schema.sql, lines 270–287)
---------------------------------------------------------------------------------------
create table games (
  id              uuid primary key default gen_random_uuid(),
  league_id       uuid not null references leagues(id) on delete cascade,
  home_team_id    uuid not null references teams(id),
  away_team_id    uuid not null references teams(id),
  scheduled_at    timestamptz not null,  -- always UTC (ADR-007)
  started_at      timestamptz,
  ended_at        timestamptz,
  status          text not null default 'scheduled'
                    check (status in ('scheduled', 'live', 'final', 'cancelled', 'postponed')),
  home_score      integer not null default 0,
  away_score      integer not null default 0,
  venue           text,
  notes           text,
  created_at      timestamptz not null default now(),
  updated_at      timestamptz not null default now(),
  check (home_team_id != away_team_id)
);


COLUMNS ADDED BY 20260415000004_game_fields.sql
------------------------------------------------
  game_stage              text not null default 'regular'
  exclude_from_awards     boolean not null default false
  is_default_result       boolean not null default false
  default_winner_team_id  uuid references teams(id)
  default_loser_team_id   uuid references teams(id)
  default_reason          text
  game_mode               text not null default 'timed'  -- 'timed' | 'untimed'
  period_type             text  -- 'quarters' | 'halves'
  period_count            integer
  period_minutes          integer
  overtime_minutes        integer
  player_of_game          uuid references players(id)


COLUMNS USED BY ScoreHeader.jsx / LiveStatTracker.jsx BUT MISSING FROM ALL MIGRATIONS
--------------------------------------------------------------------------------------
The following game columns are read/written by the live components but have NO migration:

  game_rules          jsonb   -- FIBA foul threshold rules object
  clock_running       boolean
  clock_started_at    timestamptz
  clock_time_left     numeric
  clock_period        integer
  period_status       text    -- 'active' | 'completed'
  possession          text    -- 'home' | 'away'
  home_team_fouls     jsonb   -- map of period-key → foul count
  away_team_fouls     jsonb   -- map of period-key → foul count
  home_timeouts       jsonb   -- map of segment-key → timeouts used
  away_timeouts       jsonb   -- map of segment-key → timeouts used
  entry_type          text    -- 'digital' (used by pogCalculator)
  edited              boolean -- used by pogCalculator

ACTION REQUIRED: A new migration must be created to add all missing columns above.
